let name='Sneha';
let job='Frontend Developer';


info=`Hi, iam ${name}, a ${job}.`;
console.log(info);

let text='javaScript';
let letter=`${text}.is a programming language`;
console.log(letter)
let letter2=`${text} is a programming language`;
console.log(letter2);
